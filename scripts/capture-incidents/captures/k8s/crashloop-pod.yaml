apiVersion: v1
kind: Pod
metadata:
  name: crashloop-demo
spec:
  containers:
    - name: crasher
      image: busybox
      command:
        ['sh', '-c', "echo 'starting up'; sleep 2; echo 'fatal error, exiting'; exit 1"]
  restartPolicy: Always
